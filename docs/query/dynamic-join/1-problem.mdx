# Problems with Other Solutions

import TabItem from '@theme/TabItem';
import Tabs from '@theme/Tabs';
import DynamicJoinProblem from '../../_shared/dynamic-join-problem.mdx';

In the [previous doc](../dynamic-where), we discussed dynamic queries. In this section, we will explore the problems with dynamic table joins.

<DynamicJoinProblem/>
